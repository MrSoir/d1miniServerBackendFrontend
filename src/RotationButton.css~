

.RotatingButton{
  background-color: var(--Nili-color); /* Green */
  border: none;
  color: white;
  padding: 10px 15px;
  margin-left: 10px;
  text-align: center;
  font-size: 16px;
  font-weight: bold;
  cursor: pointer;
  transition-duration: 0.5s;
  animation-duration: 0.5s;
}
.RotatingButton:hover{
	background-color: #ff00ff88;
	color: black;
}

@keyframes buttonAnimation {
   0%   {transform: rotate(0deg);}
  50%   {transform: rotate(180deg);}
  100%  {transform: rotate(360deg);}
}